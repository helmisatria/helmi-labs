import vikeReact from "vike-react/config";
import type { Config } from "vike/types";

export default {
  // https://vike.dev/head-tags
  title: "Face Recognition - Helmi Labs.",
  ssr: false,
} satisfies Config;